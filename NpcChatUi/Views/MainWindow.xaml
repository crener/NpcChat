<Controls:MetroWindow x:Class="NpcChat.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:nodes="clr-namespace:NpcChat.Views.Editors.Tree.Nodes"
        xmlns:data="clr-namespace:NpcChat.ViewModels.Editors.Script"
        xmlns:script="clr-namespace:NpcChat.Views.Editors.Script"
        mc:Ignorable="d"
        Title="MainWindow" 
        Height="350" Width="525"
        MinHeight="120" MinWidth="200"
        
        GlowBrush="{DynamicResource AccentColorBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ToolBarTray Margin="0,0,0,0" AllowDrop="False" IsLocked="True">
            <ToolBar Margin="5,0,0,0" >
                <Menu>
                    <MenuItem Header="File">
                        <Menu>Save</Menu>
                        <Menu>Save As..</Menu>
                        <Separator/>
                    </MenuItem>
                    <Button>About</Button>
                </Menu>
            </ToolBar>
        </ToolBarTray>
        <!--<nodes:BaseNode/>-->

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="42"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid VerticalAlignment="Top" Grid.Row="0" Grid.RowSpan="2">
                <Grid.RowDefinitions>
                    <RowDefinition MaxHeight="20" Height="20"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <Label Content="Story Elements" Grid.RowSpan="2"/>
            </Grid>

            <ListView ItemsSource="{Binding Speech}" Grid.Row="1" Margin="5" SelectionMode="Single">
                <ListView.Resources>
                    <DataTemplate DataType="{x:Type data:CharacterDialogModel}" >
                        <script:CharacterDialog/>
                    </DataTemplate>
                </ListView.Resources>
            </ListView>
        </Grid>
    </Grid>
</Controls:MetroWindow>
